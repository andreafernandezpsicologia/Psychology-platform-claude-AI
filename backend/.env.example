# Supabase
SUPABASE_URL=https://TUPROYECTO.supabase.co
SUPABASE_SERVICE_ROLE_KEY=tu_service_role_key

# JWT (genera 32 caracteres aleatorios: https://generate-secret.vercel.app/32)
JWT_SECRET=cambia_esto_por_un_secreto_aleatorio_largo

# Bootstrap (solo para crear la cuenta admin por primera vez)
# Una vez creada la cuenta, ELIMINA esta variable de entorno en producción
BOOTSTRAP_SECRET=cambia_esto_por_un_secreto_unico_largo

# Resend
RESEND_API_KEY=re_tuApiKey
FROM_EMAIL=Studio Renacer <admin@studiorenacer.com>

# URLs
FRONTEND_URL=http://localhost:5173
PORT=3000

# Cron jobs (genera un secreto aleatorio: https://generate-secret.vercel.app/32)
# Ponlo también en cron-job.org como cabecera: x-cron-secret: <valor>
CRON_SECRET=cambia_esto_por_un_secreto_aleatorio

# Entorno
NODE_ENV=development
